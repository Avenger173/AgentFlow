fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.7.0,<3.0.0
PyYAML>=6.0.0,<7.0.0
httpx>=0.27.0,<1.0.0
# PDF/DOCX 只读解析：Document Agent 不依赖 OCR，也不写回用户原始文件。
PyMuPDF>=1.24.0,<2.0.0
python-docx>=1.1.0,<2.0.0
# 项目方案交付使用确定性 PPTX 渲染，不依赖本机安装 Microsoft PowerPoint。
python-pptx>=1.0.2,<2.0.0
# 数据工作台 D1 使用成熟的表格解析器做受控读取与数据画像；不引入任意公式、宏或脚本执行。
pandas>=2.2.0,<3.0.0
openpyxl>=3.1.0,<4.0.0
matplotlib>=3.9.0,<4.0.0
Pillow>=10.0.0,<13.0.0
# 知识库 K1 只使用嵌入式 Chroma PersistentClient，不启动向量数据库服务或开放端口。FastEmbed
# 的模型权重保持按需下载，安装依赖本身不读取客户资料，也不自动下载 BGE 模型。
chromadb==1.5.9
fastembed==0.8.0
