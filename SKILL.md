---
name: agentflow-project
description: 开发 AgentFlow 项目时使用：本仓库是 C++ Qt 桌面端 + Python FastAPI 后端的多 Agent 工作流平台。适用于项目规划、编码、测试、文档更新、前后端联调、Agent Registry、工作流引擎、插件系统、编码检查和打包发布准备。
---

# AgentFlow 项目开发准则

## 开发前先读

每次写代码前，先快速查看这些文件，避免偏离当前阶段：

- `README.md`：项目定位、阶段路线、运行和验证命令。
- `docs/PROJECT_STATUS.md`：当前进度、已完成能力、下一步任务。
- `docs/DEVELOPMENT_ROADMAP.md`：MVP、Beta、长期版的阶段门槛和当前下一步。
- `docs/AGENT_ENGINEERING_GUIDE.md`：Agent 执行模式、评估、上下文、工具调用、失败恢复和成本控制准则。
- `AgentFlow_初版规划.md`：涉及架构、Agent、工作流、插件、安全、打包时必须参考。
- 涉及 Qt 页面结构、视觉、交互、动效或无障碍时：`docs/前端设计可借鉴文档.md`。新页面或较大信息架构改动要阅读全文；小型 UI 修复至少核对 `docs/DEVELOPMENT_ROADMAP.md` 的“前端体验验收基线”。

## 开发顺序

除非用户明确要求改顺序，否则按下面路线推进：

1. 保持现有 Qt 根目录工程可编译、可运行。
2. 在 `backend/` 内建设 FastAPI 后端。
3. 先跑通 `/health`、`/api/agents`、`/api/chat`、`/ws/tasks/{task_id}`。
4. 先使用模拟数据完成前后端通信闭环。
5. 再实现 Agent Registry 和内置 Agent manifest。
6. 再实现 Commander 初版规划。
7. 再实现 Workflow Engine 和 SQLite 状态持久化。
8. 模拟闭环稳定后再接真实 LLM。
9. MVP 稳定后再扩展文件、RAG、插件、视觉、打包。

## 写代码前检查

- 每次动代码前先重新查看本文件，尤其是“注释规范”和“写完代码后检查”。
- 先读目标文件和附近代码风格。
- 检查文件是否 UTF-8，尤其是包含中文 UI 文案的 C++/Python/Markdown 文件。
- 涉及界面布局、控件层级、可视结构时，优先在 `mainwindow.ui` / Qt Designer 中维护；C++ 只负责数据绑定、状态更新和业务逻辑。
- 临时验证可以少量动态创建控件，但稳定后要迁回 `.ui`，避免界面越来越难改、难审查。
- 未经用户确认，不把 Qt 工程迁到 `client-qt/`。
- 如发现用户刚改过的内容，先理解并保留，不要覆盖。

## 写代码时要求

- 做产品判断时要站在最终用户角度思考：这个功能是否好理解、好发现、少打断、能解释失败原因、能保护用户数据和时间。
- 站在用户角度不等于无条件照做每个临时想法；如果用户想法和产品一致性、安全性、可维护性或真实使用体验冲突，要先给出更稳妥的实现方式，并说明取舍。
- 一级产品功能不能只是把“摘要、问答、改写、提取、提纲”等通用 LLM 能力换成按钮或下拉项。这些能力若不能独立交付真实结果，优先作为 Agent 内部 Tool、步骤、配方或验证环节，不占用主页面和产品卖点。
- 功能立项坚持“功能在精不在多”：先证明它解决具体客户任务、能产生可使用的文件/数据/状态变化，并说明为什么普通聊天窗口不能等价替代，再讨论是否开发。
- 提出新 Agent 或一级功能前，必须写清：目标客户与真实场景、输入、可交付结果、不可替代性、Agent/Tool/平台服务归属、技术依赖与限制、验证方式、权限与失败处理、MVP 明确不做什么、后续扩展点。
- 用户认可产品方向不等于批准全部功能立即实现。方向下的每个 MVP 仍要逐项给出可选择方案，由用户确认范围后再写正式功能代码。
- RAG/知识库默认属于平台检索服务，Evaluator/Verifier 默认属于所有 Agent 共用的 Harness 验证层；除非存在独立任务所有权、工具面和权限边界，否则不单独包装成面向客户的 Agent 或应用页。
- 每轮开发必须对照 `docs/DEVELOPMENT_ROADMAP.md` 的当前阶段门槛；除非用户明确改路线，否则不要跳到 RAG、插件市场、视觉、多平台或真正单文件 exe 这类长期目标。
- 当 Agent 架构、工具治理、命令权限、上下文压缩、子 Agent 编排或长任务预算拿不准时，可以参考 `D:\CC\claude-code-main` 的文档和源码；只吸收工程思想、边界设计和失败处理，不直接搬代码或让 AgentFlow 偏离当前技术栈。
- UI 和流程设计优先减少用户困惑：状态要清楚、危险操作要可撤销或需确认、长耗时操作要有反馈、错误信息要能指导下一步。
- 后端 API 使用 Pydantic 模型保持结构稳定。
- Agent、Tool、Workflow、Plugin 的边界要清楚。
- 正式实现每个小 Agent 之前，必须先和用户讨论并确认 Agent 方案，不允许直接开写 Agent 功能代码。方案至少包含：目标用户、核心任务、明确不做什么、基础/付费/可定制属性、可调用工具、权限边界、输入输出协议、失败处理、验收标准和后续扩展点。
- 未经用户同意，不把临时 mock、演示按钮或规划字段升级成某个 Agent 的正式能力；先用文档或简短方案把边界讲清楚，再进入实现。
- 不按页面数量、营销名称或动作数量拆 Agent。只有职责所有权、工具面、权限策略、模型策略或输入输出契约发生实质变化时，才新增专业 Agent；一步可确定完成的能力优先定义为 Tool。
- 阶段 5 采用纵向闭环推进：先把一个经确认的 Agent 从用户入口、模型/工具循环、状态、权限、产物、验证到 UI 跑通，再讨论下一个 Agent。除非首个闭环确实缺少平台能力，不继续横向扩充底层。
- 内置 Agent 第一版优先采用“Agent Definition + 通用 AgentRunner + 受控 Tool”的结构。角色差异放在指令、工具白名单、模型策略、权限和输出契约中；不要为每个 Agent 复制一套循环。动态导入第三方 `agent.py` 属于后续插件隔离能力，不是阶段 5 前置条件。
- 不把 manifest 的 `enabled`、页面可见或 Node Contract 存在当成 Agent 已实现。Commander 的可执行路由必须检查独立的 `runtime_ready`，并保留 health/maturity 供 UI 和审计说明。
- 阶段 5 默认采用 manager 模式：Commander 保持任务和最终回复所有权，把小 Agent 当作有边界的专业能力调用；只有确实需要专家接管后续对话时，才考虑 handoff。
- Agent 可以有语言风格或人格设定，但人格只影响表达方式、提示密度和交互节奏，不允许影响事实判断、权限策略、安全边界、工具能力和验证标准。
- 权限确认策略属于平台安全层和用户配置，不属于模型自我授权；即使用户开启高权限模式，也必须保留风险提示、审计记录和随时关闭入口。
- 按 AI Harness Agent 思想拆层：Model 只负责生成，Harness 负责 Context、Planner、Tool、Runtime、Memory、Verifier、Governance。
- DeepSeek、OpenAI、Claude、Qwen、本地模型等都只能作为 ModelGateway 的供应商 profile，不要把具体供应商逻辑写进 Agent / Workflow / Tool 层。
- 引入外部 Agent Runtime 或 SDK 前，必须先核验官方文档、正式包、平台矩阵、许可证、进程模型、打包体积和 API 稳定性；只能通过 `ExecutionBackend`/Adapter 接入。AgentFlow 必须保留 task、权限、审计、产物、Verifier 和多供应商所有权，不能因某个 Harness 功能更全就整体替换控制平面。
- 供应商的上下文缓存命中率必须以响应中的实际 hit/miss token 统计，不得把“默认开启”“稳定前缀有利于命中”写成 100% 保证。动态 prompt、Tool schema、插件、时间和压缩摘要变化都要纳入缓存失效分析。
- 工作流计划、步骤输出、任务日志都使用结构化 JSON。
- 模型最终输出契约必须按当前 `output_mode` 只声明交付所需的最小字段，不能要求模型同时填满统一 Pydantic 模型的所有可选数组；设计字段数量和正文上限时同步设置受控输出预算。若供应商返回 `finish_reason=length`，要在脱敏任务说明中标明截断可能，并为长材料交付保留隔离真实模型回归夹具。
- 任务状态和日志要尽早可见，避免“黑盒执行”。
- **多阶段恢复状态机**：Map、Reduce、导出等阶段完成不等于整个 Workflow 完成。恢复请求把任务置为 `pending` 后，Runtime 必须识别已完成阶段并直接进入下一个未完成阶段，不能因沿用全局状态而停在“等待执行”。每个可恢复长任务至少覆盖“前置阶段完成 -> 后置阶段失败 -> 用户继续 -> 不重跑前置阶段”的离线回归；`cancelled` 仍是终态，不能未经明确新操作擅自复活并继续消耗额度。
- **长任务模型故障**：不得把超时、连接失败、HTTP 拒绝和 JSON 契约失败统一写成“模型输出失败”。Runtime 必须把脱敏后的失败类别和短原因写入 checkpoint、任务事件与客户可见结果；只对连接、超时、限流或短暂服务端错误做至多一次有记录的退避重试，参数/HTTP 400/输出契约错误立即停驻。回归至少覆盖“首次瞬态失败后自动成功”和“第二次仍失败后不再调用”的两条路径，避免客户靠连续点击碰运气或系统无界消耗额度。
- **供应商限流恢复**：当 Provider 明确返回 RPM/Retry-After 时，长任务必须按同一 Provider、模型和脱敏账户键建立进程内请求队列，在安全窗口自动继续；不允许用固定毫秒级退避反复撞 `429`，也不允许让客户用连续点击替代限流调度。等待期间要保持真实运行态、显示预计等待和已保存 checkpoint；未提供明确限流信息时才使用一次短退避并停驻。
- **供应商错误脱敏**：Provider 错误摘要进入 UI、事件或 SQLite 前，必须移除 API Key、访问令牌和账户标识的疑似片段，包括 `sk-`、`ark-`、`ak-` 及尖括号包裹形式；保留 HTTP 状态、错误类别和有限的可操作原因即可。
- 真实 Agent Runtime 要记录任务成功率、步骤数、耗时、工具调用状态、失败原因和成本估算，避免只看模型回答是否流畅。
- 工具调用必须有结构化参数校验、超时、失败计数和重试上限；同类失败连续超过阈值时要停止自动循环并给用户可理解的说明。
- 对于串行的长耗时外部 Provider（如多张 AI 图片生成），客户端总超时必须覆盖“单次上限 × 最大次数 + 文件渲染/回读余量”，并保持异步 UI；不能出现产物已写出而客户端先报超时的假失败。每个外部调用仍保留独立上限、数量上限和可降级路径。
- 模型连接测试运行时和正式任务运行时必须分离。连接测试可以使用很小的 token/timeout；Agent 内部 provider 路由或后备模型不得直接复用该预算，否则长 JSON 会被截断并被误判为模型能力不足。
- 联网取得的搜索服务生成答案、百科概述或普通总结默认只算“可追溯参考”，不能冒充已核验图表数据。只有产品明确标注“联网核验”时，才允许把每个 PPT 数值回指实际来源 ID 和原文片段。**普通创作型 PPT 默认采用已配置模型直接生成可编辑数据底稿，不联网、不等待来源验证，也不把数值表述为已核验事实。**
- PPT 数据增强按用途分级，不能拿审计级门槛阻断普通演示交付：客户明确要求表格或图表时，Harness 必须建立类型与数量合同，模型直出数据只要满足受控对象/指标、有限数值和原生图表形态即可交付；不能因为缺少网页证据静默交付 `0` 张表/图。只有客户显式选择“联网核验”时才启用来源、单位和期间的严格审查。导出后仍必须实际回读 PPTX 的原生 Table/Chart 及其内嵌数值。
- PPT 客户明确要求多种数据、多个表格、柱状图或折线图时，验收对象是这些实际交付物，而不是“模型调用成功”或 artifact 写入成功。规划器要主动补齐有限的候选指标、查询和数据点预算；研究结果按视图独立降级，不能把全部要求收束成一条数据，也不能因一张图不成立撤回其它已成立视图。
- PPT 数据规划遵循“简短输入主动扩展、详细输入优先遵从”：客户只说主题或“生涯数据”时，专用规划模型应决定 3 至 5 个互补视图、指标分组和合理数据量；客户点明页数、图表类型或数量时，Harness 将其保存为硬合同。不能把功能固定成一张表、固定页数或只靠“表格/柱状/折线”关键词判断；表格、横向条形、分组柱图、折线、面积、饼图和环形图按数据形态选用，饼/环图仅用于真实构成关系。
- PPT 数据规划中的客户明确对象同样属于硬合同：从客户原句可稳定提取的单对象或对象对，必须在主创作提示、研究蓝图规范化和数据模型上下文三层保持一致。模型不得因高频联想新增人物、品牌、城市或产品；若单对象数据不适合分组柱图，Harness 应换成可落地的画像/趋势视图，而不是静默少交付一张图。
- PPT 的“至少 N 张表/图”先由 Harness 从客户原话提取为数量合同，再让规划模型补充指标和查询；重复表达取最大值，不能相加或交给模型自行缩减。多个表格必须绑定不同指标组或趋势底稿，不能复制同一张表凑数量。导出后按原生 Table/Chart 类型逐类计数；未满足明确合同只能报告部分完成，并写入 artifact 缺口。
- 客户要求“可编辑表格/图表”时，必须验证交付文件中的原生对象类型：PowerPoint 表格用 Table，柱图/折线图用 Chart，并回读其内嵌数据；用矩形、连线或文本模拟出相似外观不能算完成。
- PowerPoint 原生动画/转场的 XML 存在、shape 目标正确和 `python-pptx` 可回读都只是工程前置验证，**不能替代实际 PowerPoint 放映验收**。只有客户在实际放映中确认可见的效果才算交付；若客户只看到转场或效果不符合预期，必须如实标为未完成/暂停项，不能因 Timing XML 已写入而在状态文档中宣称完成。
- 多点模型抽取采用逐点验证：无来源、数值不匹配或无法定位原文的点单独丢弃并说明，合法点继续参与图表口径校验；不能让一条坏引用撤回所有其它合法数据，也不能为了保住视图放行整批未验证数据。
- PPT 联网研究优先采用“共同查询 + 各对象聚焦查询”的有限并发聚合；整项最多一次受控补查和两次抽取模型调用。首次已取得来源时先修结构，不为满足形式审查反复搜索、无限重试或叠加无收益模型回合。
- 开发 PPT 智能研究、数据表格或图表时，先完整阅读 `docs/PPT_RESEARCH_DATA_DESIGN.md`。创作规划与数据规划必须职责隔离：主模型只负责内容和版式，专用规划器只生成对象、指标、图表合同和数据点预算；确认导出后默认由数据模型直接填充该蓝图，**不调用网页搜索**。客户明确选择联网核验时，固定 Provider、搜索 API 和 MCP 才能通过统一 ResearchGateway 使用，不能按每个客户主题不断往 PPT 业务层堆专用 MCP。
- 修改真实模型规划、AI 数据直出、原生图表渲染或外部图像 Provider 后，除离线回归外必须在用户已授权额度的前提下运行 `backend/scripts/verify_live_presentation_studio.py --live`。真实验收只输出 provider、图表类型/数量、数据点数量和图片数量，绝不输出 API Key、原始响应或敏感正文；失败先按实际阶段修复，不用提高轮数或伪造成功掩盖。
- Runtime、外部代码工具集成或任何专业 Agent 涉及命令执行时，必须先做命令风险分级：只读定位、诊断验证、修改型、联网、高危。命令运行要记录目的、工作目录、参数、超时、输出截断、失败原因和权限决策；修改型、联网和高危命令不得绕过用户的权限确认策略。
- 2026-08-14 已确认不自研通用代码工坊 Agent，不得因为仓库中仍有旧 manifest、Node Contract 或 UI 占位而自行恢复立项。未来若接入 Codex、Claude Code 等成熟代码 Agent，应作为受控外部能力 Adapter 单独讨论范围、权限、数据边界和验收，不把它伪装成 AgentFlow 自研能力。
- DeepSeek Harness H2 以后按真实需求启用：H2 可以做隔离只读工程试点，但接入客户任务必须先有已确认的专业 Agent 或总指挥场景；H3 写入/批准、H4 MCP 仅在该场景确实需要时开发；H5 只在发行版本决定携带 Harness 时进入。不得为了完成框架阶段而制造产品功能。
- 开发数据工作台前必须完整阅读 `docs/AGENT_SPECIFICATIONS.md` 的“003 数据分析与表格交付 Agent”。模型只规划和解释已经由确定性 Tool 计算、校验的数据；禁止让模型计算数值、生成 Python/SQL/公式交给 Runtime，禁止联网补数或修改源文件。正式交付的 `.xlsx` 必须回读原生 Table/Chart、指标单元格和数据范围后才能登记 artifact。
- 高危命令默认禁止或强确认，包括删除/覆盖目录、`git reset --hard`、强制 checkout、执行未知下载脚本、提权、修改系统路径等；即使用户开启高权限模式，也要保留风险提示和审计记录。
- 长对话和长任务优先用摘要、子任务拆分和数据库恢复上下文，不要把全部历史直接塞进模型窗口。
- 调度台的连续会话与跨会话长期记忆必须分层：同一 `conversation_id` 的完整脱敏聊天归档、材料范围、计划和摘要可以自动维护；模型 Prompt 只读取有界摘要和最近消息，不能把“完整历史可见”误做成无限上下文。跨会话偏好、项目约束和经验必须可见、可关闭、可编辑、可删除，不能把完整对话或私有材料静默升级为永久记忆。
- 每个 Agent、Agent action 或关键专业功能的模型选择必须有可审计的显式路由：客户可选择继承全局配置或已配置的兼容 provider/model/thinking Profile；不得把静默后备、兼容性降级或模型替换伪装成客户原选择。
- 调度台的 `@Agent`、材料标签或未来 MCP/Skill 提示只表达路由偏好与受控材料范围，不能扩大权限、绕过 action 准入、自动联网或把未实现的专业能力承诺给客户。
- Commander 的自然语言回复、结构化计划和真实 Runtime 必须来自同一份已校验事实：dry-run 只能陈述已规划的能力、材料范围、待确认与未执行边界；不得声称读到了未执行的专业结果，也不得否认已明确绑定且通过准入的材料。
- 启动期或依赖准备期不能把客户提交直接交给未 ready 的本地后端并等待长网络超时。Qt 应保持可输入，在 50-100ms 内显示“已暂存/正在准备”，后端 ready 后安全自动提交；失败时必须保留原输入和材料并给出可执行提示。
- 严格区分模型可见上下文和 Runtime 本地上下文。认证信息、数据库连接、日志器、权限决策、绝对路径和内部对象只给代码/工具使用；模型确实需要的事实再通过输入、检索或脱敏工具结果显式提供。
- 人工批准是同一次运行的可恢复暂停，不是新建任务或新开一轮对话。权限请求必须保存待审批工具调用和恢复状态，批准/拒绝后从原 `runtime_task_id` 继续，并保持审计链连续。
- 涉及代码库、日志、配置、报错、函数名、类名、路径、ID、文件名这类“精确定位”问题时，默认先用 `rg/ripgrep`、glob、read file 等 agentic search 工具按需检索；涉及模糊问答、跨文档归纳、知识解释、语义召回时，再引入 RAG 或摘要层。
- RAG 不是 Agent 必需组件，而是检索工具箱中的一种实现。未来做知识库和代码检索时采用“agentic search first，RAG as needed”的路线：先 grep/ripgrep 粗筛范围，再由 RAG 在必要时做语义召回、重排、总结和上下文补全；不要把整个代码仓库默认全量向量化。
- 文档 Agent、知识库能力或任何需要检索代码、配置、日志的专业任务在设计检索链路时，要先区分“定位”与“理解”两类诉求，不允许把所有查询都塞给单一 RAG，也不允许把只会字符串匹配的 grep 当成完整智能问答方案。
- 2026-08-20 用户已批准阶段 5K 本地知识库方案；开发知识库前必须完整阅读 `docs/KNOWLEDGE_BASE_PRODUCT_SPEC.md`。该批准只覆盖 K0-K5 既定范围，不自动批准 OCR、网页爬虫、云连接器、知识图谱或全仓库向量化；每一步必须按 K0 技术证据和阶段验收推进。
- 涉及文件写入、命令执行、联网、插件安装、数据库修改时，先设计权限字段和确认流程。
- UI 网络请求、进程启动、WebSocket 都不能阻塞 Qt 主线程。

## 性能规范

- 不在高频请求路径里反复做磁盘扫描、重复解析大文件或重复创建昂贵对象；需要时用轻量缓存，并说明失效策略。
- Agent Registry、插件目录、模型配置这类低频变化数据，优先用 `mtime/size`、版本号或显式刷新做缓存失效。
- 大模型调用、文件解析、Workflow 执行必须异步或后台执行，不阻塞 FastAPI 事件循环和 Qt 主线程。
- 并发步骤消耗共享预算时，必须在进入并发执行前做额度预留或原子扣减；不能让多个分支先分别通过预算检查、完成后再累计，避免并发超支。
- 性能优化先处理明显瓶颈和可预期增长点，不为了“看起来高级”提前引入复杂缓存系统。
- 新增性能相关逻辑时要写注释，说明为什么缓存、何时刷新、最坏情况下会不会返回过期数据。
- 检索和知识库性能优先级顺序：先减少无意义扫描和重复读取，再做轻量缓存，再评估索引；RAG 只为真正需要语义理解的文档建索引，不为“能搜到字符串”的内容平白增加 embedding 和重建成本。

## UI 交付补充

- 每次 Qt UI 改动都以 `docs/前端设计可借鉴文档.md` 为参考，但要按 Qt Widgets 的能力落地，不机械照搬 CSS / Web API 名词。优先吸收任务工作台、渐进披露、显式状态、证据可追溯、性能和无障碍原则。
- 做 Qt 页面前先想清楚四件事：目标用户在这页最常做什么、第一眼要看见什么、成功/失败/等待时怎么提示、长文本或窄窗口下会不会遮挡。
- 页面设计先保证信息架构和状态表达，再追求视觉细节；不要为了“内容塞得多”把摘要、按钮、权限条、输入框彼此压住。
- 长结果、来源、日志、配置和操作不强塞进同一个页面：主页面承担当前任务，完整阅读、审计或编辑使用详情页、Inspector 或明确的子视图；预览与详情必须展示同一份已验证数据，避免产生两套事实。
- UI 验收至少覆盖：空闲、运行、等待输入/权限、成功、部分完成或失败；状态不得只靠颜色表达，必须给出文字和下一步；每个区域通常只保留一个主要操作；用户结果要有充足可伸缩的阅读空间，不能被操作控件挤压。
- 动效只能解释真实状态或空间变化，必须可打断且不阻塞交互；没有可信进度时展示阶段，不伪造百分比或无限循环动画。优先保证启动、滚动、筛选和长结果渲染流畅，再考虑装饰效果。
- **统一任务活动标志**：凡是 Agent、索引、导入、导出或后台检查任务，状态区域都必须放置可复用的活动指示器。仅当后端真实状态为 `queued`、`pending` 或 `running` 时允许转动；`paused`、`blocked`、`completed`、`failed`、`cancelled` 必须立刻停止。阶段字幕用于解释正在做什么，不能再被客户当作判断“任务是否结束”的唯一依据；新页面默认复用该组件，不重新各写一套加载动画。
- **操作层级与可点击性**：工作区的命令按钮必须使用已有的 `primaryButton`、`secondaryButton`、`ghostButton` 或 `dangerButton` 语义样式，不能以无边框普通文字充当关键操作。每个操作区只保留一个填充色主按钮；暂停、查看、关闭等次操作使用可见边框或浅色底；取消、清空等破坏性操作使用红色危险样式。`blocked` / `paused` 状态下可恢复动作必须成为填充色主按钮，并直接说明会从何处恢复、是否会重做已完成步骤。按钮还必须覆盖 hover、pressed、focus、disabled 四态，禁用原因通过 tooltip 或相邻说明可理解。
- **默认按钮兜底**：除左侧导航、纯图标工具和明确的文本链接外，未标注语义的 `QPushButton` 也必须默认使用可见的白底细边框，不能因为遗漏 `objectName` 而退化成不可辨识的文字；导航可由 `sidebarFrame` 局部覆盖为无边框样式。新增页面先为每个按钮指定语义，再依赖该兜底处理历史页面。
- 每次改 UI 后至少自查这几类状态：空状态、加载中、错误提示、长文本、权限警告、分页或筛选后内容变化、窗口缩放后是否重叠。
- Qt 编译通过不等于页面已交付。涉及页面高度、布局伸缩、隐藏/显示控件或信息层级时，必须在实际窗口至少检查首屏：Hero 不得吞掉结果空间，标题/图标/主操作应完整可见，工作区必须保有可阅读结果的伸缩空间。
- 交互控件要让用户一眼看懂哪里能点、当前处于什么状态、下一步该做什么；危险动作必须能解释风险，长耗时动作必须有反馈。
- 后台或脱离请求执行的任务必须在完成、失败、取消和阻塞时主动发出终态事件；不能只更新某个隐藏状态表，让用户靠手动刷新才知道结果。
- 保持现有 AgentFlow 的统一视觉语言：标题层级、图标、渐变 hero、卡片角色、留白节奏要延续，不要每个页面各自一套风格。

## 注释规范

- 代码注释以中文为主，关键协议名、类名、字段名保留英文。
- 不写“把 A 赋给 B”这类无效注释。
- 在这些位置主动补注释：
  - 前后端协议转换处，例如 JSON 到 C++ 结构体、Pydantic 模型。
  - 异步生命周期处，例如 `QNetworkReply`、`QWebSocket`、`QProcess` 的连接、关闭、错误处理。
  - 工作流调度、任务状态迁移、权限判断、安全边界。
  - 以后自己或他人容易误改的兼容逻辑。
- 修改旧代码时，如果发现逻辑已经不直观，可以顺手补一两句说明。
- 注释要帮人理解“为什么这样做”和“边界是什么”，不要堆满每一行。
- 新增核心类时，头文件至少要说明类职责、关键 public 方法语义、重要信号含义和状态字段边界。
- 新增异步/生命周期类时，源文件至少要说明启动顺序、失败路径、重试策略、资源归属和退出清理策略。

## 写完代码后检查

- 运行最小但有效的验证命令。
- 如果构建工具不在 PATH 中，要明确记录，不要假装已通过。
- 检查中文 UI 文本、JSON、Markdown 是否有乱码。
- 阶段、运行命令、已完成能力变化时更新 `docs/PROJECT_STATUS.md`。
- 架构、安装方式、路线变化时再更新 `README.md`。

## Git 版本管理

- 每轮完成实际代码或文档改动后，先完成对应的最小有效验证、检查 `git diff --check`，再创建一条只包含本轮已验证改动的本地 Git 提交。
- 提交信息应说明客户可感知的结果或工程修复，不使用“update”“fix”等无法追溯的空泛信息；不得把无关的用户改动、构建产物、缓存、模型、输出目录或 `.env` 一并提交。
- 本地提交成功后，默认立即推送到已配置的 `origin` 当前分支；推送前仍需确认 remote URL 不含凭据，提交前检查暂存内容不含 API Key、访问令牌、私人材料正文或其他敏感数据。
- 网络、GitHub 认证或远端保护规则导致推送失败时，不重写、不回退本地提交；记录简短失败原因并在下一轮优先重试推送。只有用户明确要求时才 amend、rebase、force push 或清理历史。

## 复盘与规则升级

- 用户明确纠正我、拒绝某种做法、指出重复问题时，要先找根因，再决定是否把经验升级进本文件或相关文档，避免同类失误反复发生。
- 对自己刚完成的重要改动，要做一次简短自查：目标是否达成、是否引入了新混乱、还有没有明显更稳的写法。
- 只有“可复用、跨多轮有效”的教训才升级为规则；一次性的上下文指令不要硬写成全局规范。
- 不从用户沉默里臆测偏好；只有用户明确表达，或同类证据重复出现时，才把它沉淀为长期规则。
- 需要沉淀的复盘内容优先记录“为什么会错、下次怎么避免”，不要只记录表面现象。
- 外部工程按证据等级使用：官方文档/协议优先，其次是公开源码和本项目验证，第三方复原工程只作设计线索和反例来源。`D:\CC\claude-code-main` 不是 Anthropic 官方源码，吸收结论前要结合其测试、审查报告或 AgentFlow 自己的验证。

## 验证纪律与少走弯路

- 遇到构建失败时先读错误的第一处根因，不要立刻换一堆命令反复试；每次重跑前要明确“这次验证什么假设”。
- 手写 `.ui` 时不要给 `QBoxLayout` 添加 `<property name="stretch">`；Qt 6 `uic` 会生成不合法的单参数 `setStretch` 调用。应在 Designer 为具体 layout item 设置 stretch，或在 `setupUi` 后用 `setStretch(index, value)`，随后先跑一次 AutoUic 构建。
- Qt 的 `signals`、`slots`、`emit` 等可能是预处理宏；C++ 局部变量、字段和方法命名不要复用这些词。遇到看似普通字符串行报 `C2059` 时，先检查 Qt 宏冲突，而不是盲目改字符串拼接。
- Qt Creator Debug 构建目录当前使用 `NMake Makefiles JOM`，`CMakeCache.txt` 里可能只有 `CMAKE_MAKE_PROGRAM=jom`。当前实际可执行文件是 `D:\IDE\qtcreator\Tools\QtCreator\bin\jom\jom.exe`；命令行重跑前把它所在目录 `D:\IDE\qtcreator\Tools\QtCreator\bin\jom` 加到 `PATH`。不要为了找 `jom.exe` 对整个 Qt 目录做长时间递归扫描。
- 命令行快速验证优先用固定的 `build/codex-debug` + Ninja；只有涉及 Qt Creator 实际运行、`uic`、退出稳定性或用户复现问题时，再跑 Qt Creator Debug 构建。
- Windows 终端区域设置为 GBK 时，`pip` 可能把 UTF-8 的 `requirements.txt` 误按本地编码读取；安装依赖统一使用 `python -X utf8 -m pip install -r backend/requirements.txt`，不要为了让 pip 通过而把项目依赖文件转成 ANSI。直接调用 CMake/Ninja 前先在同一命令中 `call D:\IDE\VS2022\buildtools\VC\Auxiliary\Build\vcvars64.bat`，否则缺少 MSVC 标准库路径的报错不能当作项目编译失败。
- 新增或依赖 Python 解析/渲染库时，验证命令必须使用桌面端 `BackendManager` 实际解析到的 Python（当前优先为 `backend/.venv/Scripts/python.exe`），不能只用系统 `python` 通过回归。安装后至少执行该解释器的 `-m pip check` 与相关专项回归；可选能力应在 `/health` 提前报告就绪/缺失状态，不能等客户导入自己的文件后才以 HTTP 400 暴露环境问题。
- Chroma `PersistentClient` 在 Windows 上重建或删除临时索引前必须显式调用 `close()`、释放 collection 引用并完成垃圾回收；只把 Python 变量设为 `None` 可能仍保留 SQLite/HNSW 文件句柄，导致系统临时目录残留。相关探针必须把“清理后目录不存在”作为成功条件，而不是忽略删除异常。
- PowerShell 显示中文前先设置 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`，并用 `Get-Content -Encoding UTF8` 读取文档；不要把控制台显示乱码误判成文件乱码。通过 here-string 或管道把中文测试材料传给 Python、Node 等子进程时，还要设置 `$OutputEncoding = [System.Text.UTF8Encoding]::new($false)`，否则进程输入可能被降级成单字节字符，造成阈值、行号或模型验收假阳性。
- 后端测试与项目根 UTF-8/文档检查不要混用相对路径：命令工作目录是 `backend/` 时，后端文件使用 `app/...`、`scripts/...`，项目根文件必须使用 `../README.md` 等相对路径；更稳妥的做法是分成两条命令，测试在 `backend/` 执行，编码与文档检查回到项目根执行。先确认 `Get-Location`，不要把 `backend\...` 再拼到 `backend/` 下。
- `git` 不一定在当前 PATH 里。需要查看改动时可以先 `Get-Command git`，不可用就不要围着 `git diff` 消耗时间，改用明确的文件检查和验证命令。
- GUI 启动/关闭烟测不要用 `Start-Process -WindowStyle Hidden`，隐藏窗口可能拿不到 `MainWindowHandle`，导致 `CloseMainWindow()` 无效并留下后端进程。需要测关闭速度时用可见窗口短测，结束后确认 `127.0.0.1:8765` 无残留监听。
- 如果 `8765` 被占用，先用 `Get-NetTCPConnection` 和 `Get-CimInstance Win32_Process` 查看命令行，确认是本项目 uvicorn 后端后再清理；不要盲目杀 Python 进程。
- 不要把大规模递归搜索当成默认排查手段；优先查已知路径、CMakeCache、项目文件或用 `rg` 精确搜索，超时搜索要立刻换更窄范围。
- 每轮开发结束只记录与本轮相关的验证结果，不把失败的临时错误堆进文档；真正有复用价值的踩坑才写入本文件或 `docs/PROJECT_STATUS.md`。
- 真实模型或长耗时网络探针被命令超时、界面中断或连接断开后，必须核对自己启动的子进程是否仍存活；只终止本轮可明确识别的探针，避免后台请求继续占用 Provider 并发和额度。不要用 1 秒一类短命令超时测试本来需要几十秒的正式任务。
- 客户已反馈额度消耗或要求避免浪费时，先用固定来源、MockTransport 和本地 PPTX 回读把协议、分组、渲染和验收全部跑通；真实模型/联网只保留一次有明确验收目标的最终探针，不用连续真实调用代替本地调试。

## 编码规则

- 新增文本、代码、配置文件统一使用 UTF-8。
- 代码标识符和文件名优先 ASCII；中文用于 UI 文案和中文文档。
- C++ 文件包含中文字符串时，CMake 必须为 MSVC 添加 `/utf-8`。
- Python 读写文本时显式使用 UTF-8，避免系统默认编码导致中文乱码。
- JSON/YAML 读回检查，确保中文没有变成乱码。

## 后端约定

后端根目录使用 `backend/`：

```text
backend/
├─ requirements.txt
├─ requirements-dev.txt
├─ main.py
├─ app/
│  ├─ api/
│  ├─ core/
│  ├─ schemas/
│  ├─ services/
│  ├─ agents/
│  ├─ workflow/
│  ├─ tools/
│  └─ database/
└─ scripts/
```

第一版先保持模拟实现，但 API 形状要接近最终真实系统，方便以后替换内部逻辑。

默认本地服务：

```text
host: 127.0.0.1
port: 8765
```

核心接口：

```text
GET  /health
GET  /api/agents
POST /api/chat
WS   /ws/tasks/{task_id}
```

## 前端约定

- 继续使用 Qt Widgets 和现有 QSS 风格。
- 新增或重排页面时优先修改 `mainwindow.ui`，复用现有 `contentCard`、`sidePanel`、`ghostButton`、`sectionTitle` 等样式角色。
- 当前运行时主样式主要写在 `mainwindow.ui` 的内联 `styleSheet` 里；修改全局视觉前先确认它是否已经加载，`styles.qss` 目前只是参考文件，不能默认认为会生效。
- 原有页面的整体视觉元素不能随意删除，包括图标、hero/背景样式、页面标题层级、卡片角色和全局配色；只删除确实无用的占位内容。
- 不在 `.cpp` 中堆完整页面结构；`.cpp` 负责拿到 `ui->xxx` 控件后连接信号、填充数据和处理状态。
- `main.cpp` 里 `MainWindow` 采用堆分配；`MainWindow::closeEvent` 负责先停止本窗口启动的后端再触发应用退出。Qt 6.11 + MSVC Debug 下显式析构庞大的 Designer 窗口树曾暴露 CRT 堆损坏风险，不要随手改回栈变量 `MainWindow window;`，也不要重新加回 `window.reset()` 一类退出期强制析构，除非重新完成启动/关闭烟测。
- `main.cpp` 已固定 `QApplication` 使用 Fusion 样式；不要随手移除。Qt 6.11 Debug 环境下 modern Windows style 插件曾在窗口初始化期崩溃，而当前视觉主要由 `mainwindow.ui` 的 QSS 控制。
- 当前已关闭运行时批量 `QGraphicsDropShadowEffect` 阴影增强；整体视觉仍由 `mainwindow.ui` 的 QSS 保持。后续若恢复动态阴影，必须同时验证 Qt Creator Debug 构建的启动和退出。
- HTTP 使用 `QNetworkAccessManager`。
- 实时任务日志使用 `QWebSocket`。
- 后端进程生命周期后续用 `QProcess` 管理。
- 复杂 Agent 控件之前，先显示后端状态、Agent 列表、聊天响应、任务日志。

## 安全默认值

- 插件代码、Shell、联网、文件写入、数据库变更默认视为高权限行为。
- 插件权限默认拒绝，用户确认后再开启。
- 危险操作即使是模拟，也要在日志里体现。
- 接入真实模型配置前，不要把 API Key 写进代码、README、状态文档或提交内容。
- 开发期模型 Key 只能放在本地 `.env`、系统环境变量或后续的加密存储里，并确保 `.env` 被忽略。
- 最终回复和日志中不要回显完整 API Key；必要时只说“已配置”。
- 检查 `.env` / `.env.example` 时只允许读取或搜索已知的非敏感字段；不要整文件输出。发现示例文件误含真实 Key 时，立即替换为占位符，并提醒用户轮换该 Key。
- 不把用户提供的 API Key、第三方隐私文档正文、无必要的敏感业务内容写入 `SKILL.md`、`README.md`、`docs/PROJECT_STATUS.md` 等长期文档；长期文档只保留抽象规则和脱敏结论。
- 当前后端模型配置已接 Windows DPAPI 第一版；后续如果扩展 Credential Manager、macOS Keychain 或 Linux libsecret，也必须保持“无安全存储则不保存明文 Key”的原则。

## 文档习惯

每一轮开发结束后，在状态文档中留下简短记录：

- 改了什么。
- 验证了什么。
- 哪些没验证。
- 下一步建议。

`docs/PROJECT_STATUS.md` 要保持短而当前化，只保留当前状态、最近验证基线、下一步和重要注意事项；详细历史或复盘写入 `docs/WORKLOG.md`，不要把每轮临时验证流水长期堆进状态文档。

文档用中文为主，英文只保留必要技术名词。
